[supervisord]
nodaemon=true

[program:consul-template]
command=consul-template -consul=consul.demo.gs:8500 -template "/etc/consul-templates/nginx.ctmpl:/etc/nginx/nginx.conf:usr/sbin/nginx -s reload"

[program:nginx]
command=/usr/sbin/nginx -c /etc/nginx/nginx.conf
autorestart=true
